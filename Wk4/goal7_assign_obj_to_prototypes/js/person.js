/**
 * Created by the JavaScript Development Team
 * Class: PWA
 * Goal: Goal7
 */

/*
    Kellie Pickering
    05/29/15
    PWA1 - Analyze Objects.person

 */

// Set the person object equal to the global window.

// Create an array of four jobs and attach it to the Person object.
    // e.g. "designer", "programmer", "animator", "astronaut"

// Create an array of actions and attach it to the Person object.
    // e.g. "walking", "studying", "working"

// Create a Person constructor to hold the following four properties:
    // Name     (=this.name)        Randomly selected name
    // Action   (=this.action)      Randomly selected action
    // Job      (=this.job)         Randomly selected job
    // Row      (=this.row)         Selected row within innerHTML

// To avoid code reuse, assign an update() function to person.prototype
    // Assign the Person.action to equal a random action from the array (using Math.random formula).
    // Use innerHTML and getElementByID to print the new action to the page.